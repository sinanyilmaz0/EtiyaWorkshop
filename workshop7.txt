1. TS'de undefined ve null nedir, aralarındaki fark nedir ? 

Undefined, o şeyin hiçbir fikrinin olmadığı yerdir; türü yok ve bu kapsamda daha önce hiç referans alınmadığı, “değer yok” anlamına gelir. 
Başlatılmamış değişkenler, eksik parametreler ve bilinmeyen değişkenler bu değere sahiptir. Null, varlığın bilindiği yer, ancak değerin ne olduğu bilinmemektedir. 


2. HTTP request nedir ? 

Bir sunucuda bulunan kaynağa erişmek amacıyla yapılan HTTP istekleridir.


3. HTTP request yapısı nedir ?

Request içerisinde Method, Path, Version Control ve Header kısmından oluşur.

4. HTTP request türleri ? 

a. Get
b. Post
c. Put
d. Head
e. Delete
f. Connect
g. Options
h. Trace
i. Patch
j. Search

5. HTTP status kodları nelerdir ?

Informational responses (100–199)
Successful responses (200–299)
Redirects (300–399)
Client errors (400–499)
Server errors (500–599)

6. En sık kullanılan status kodları

404 — Not Found,
301 — Permanent Redirect,
302 — Temporary Redirect,
500 — Internal Server Error,
503 — Service Unavailable,


7. TS'de interface nedir ? 

TypeScript’teki bir Interface, bir object için önceden kurgulanmış plan plandır. TypeScript derleyicisinin object’leri doğrulamasına yardımcı olur.


8. TS'de class (sınıf) ile object (nesne) arasındaki fark nedir ? 

Sınıf (class) soyut bir veri tipidir. Nesne (object) ise onun somutlaşan bir cismidir.


9. IoC nedir ? 

Inversion of control(IoC) bir yazılım tasarım prensibidir. Ioc ile Uygulama içerisindeki obje instance’larının yönetimi sağlanarak, bağımlılıklarını en aza indirgemek amaçlanmaktadır. 


10. Dependency Injection nedir ? 
Dependency injection kaba tabir ile bir sınıfın/nesnenin bağımlılıklardan kurtulmasını amaçlayan ve o nesneyi olabildiğince bağımsızlaştıran bir programlama tekniği/prensibidir.